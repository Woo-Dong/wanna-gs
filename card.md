# 원하GS 작업 기준 카드

고객이 원하는 상품을 말하면 한 점포의 수요로 모으고, 경영주가 적은 수고로 보수적으로 발주해 고객의 픽업까지 연결한다. 오류를 해결할 때에도 이 흐름과 자연어 품질을 유지한다.

이 카드는 작업 시작·인계·복구 때 읽는 안내다. 요구사항 원장은 [사용자 결정](docs/02-decisions-and-open-questions.md)과 [CORE_REQUIREMENTS](docs/CORE_REQUIREMENTS.md), 채택 정책은 [DECISION_INDEX](docs/DECISION_INDEX.md)에 연결된 ADR이다. 최신 사용자 지시가 우선하며 이 카드가 별도 정책을 만들지는 않는다.

## 반드시 이어져야 하는 경험

- 고객: 자연어 → 실제 카탈로그 후보 확인 → 상품·수량·점포·가격·자동 구매 동의 → 요청 → 공급 확보 후 모의 결제 → 입고·픽업 알림 → 수령. 알림 생성부터 정확히 48시간이다. (CORE-02~04, 07~10)
- 경영주: 수요·미확보 요청을 묶어 파악 → 자연어 수정·승인 또는 사전 승인한 보수적 자동발주 → 공급·배정·입고 처리. 수요·예산을 넘기거나 중복 처리하지 않는다. (CORE-05~08)
- 식별 불가: 모르는 상품을 만들어내지 않고 추가 확인·미식별 기록으로 연결한다. 알아볼 수 있는 정상 요청까지 모두 거절해서는 안 된다. (CORE-03, 11, 21)
- 시연: 실제 모델과 DB를 사용하는 고객·경영주 흐름을 Vercel 제출 URL에서 검증한다. GS 거래와 결제는 모의다. 본부·사진·링크·레시피·실결제는 추가하지 않는다. (CORE-01, 12~14, 23)

## 작업 시작과 재개

1. 이 카드, 관련 CORE·사용자 결정·유효 ADR, 현재 PROGRESS와 작업 계약을 읽는다. 새 에이전트·인계·컨텍스트 축약 뒤에도 같은 절차를 적용한다.
2. 작업 계약에 **누가 무엇을 할 수 있어야 하는지**, 관련 CORE/AC, 유지할 정상 사례와 금지할 변화를 적는다. 내부 작업은 그 작업이 지탱하는 사용자 기능에 연결한다.
3. 현재 clone/worktree 루트와 실제 branch·SHA·환경을 확인한다. 이전 실행 경로나 오래된 정책·테스트 결과를 그대로 믿지 않는다.

## 오류를 고칠 때

- 실패 원인과 사용자 목적을 나눠 기록한다. 구현이 어렵다는 이유로 기능을 삭제하거나 상태 전이·기한·동의를 바꾸지 않는다.
- 재현 사례뿐 아니라 그 수정이 막을 수 있는 **원래의 정상 사례**를 함께 검증한다. 예: 잘못된 상품 거절을 강화했다면 실제 상품의 다양한 표현도 식별되는지 확인한다.
- 테스트 기대값 변경은 요구·ADR 근거와 독립 검토가 있어야 한다. 테스트 삭제/skip, 항상 실패 반환, 화면만 성공 처리, 평가 정답 하드코딩, live를 fixture로 바꾸는 방식으로 완료하지 않는다.
- 기술 구현은 범위 안에서 바꿀 수 있다. 제품 정책 변경이 필요하면 17번 절차로 기록·검토한다. 핵심 요구 충돌을 투표나 편의로 덮지 않는다. 단순 오탈자나 정책을 바꾸지 않는 수정마다 두 정책 검토자를 요구하지 않는다.
- 확인된 원인에 맞는 최소 수정을 하고 영향받은 단위→통합→필요한 E2E를 검증한다. 실패하면 관련 병합을 보류하고 15번의 제한된 복구 절차로 돌아간다.

## 독립 검증과 완료

검증자는 작업 보고서의 ‘목적 보존’ 항목에서 원래 정상 결과, 수정한 실패 결과, 인접 기능 영향, 실제 증거를 확인한다. 구현자와 같은 주장만 반복하거나 CORE 번호만 적은 것은 증거가 아니다. 정상 흐름이 막히거나 필수 기능이 사라지면 테스트가 녹색이어도 FAIL이다.

GATE-BOOTSTRAP에서 요구 매핑·목적 보존 항목 누락과 stale 증거를 검사하고 독립 판정을 병합 조건에 연결한다. 문서가 존재하는 현재 상태는 지침만 준비된 상태이며 실행기·CI 강제는 아직 구현 전이다. 기능이 없는 초기 구축에는 현재 단계의 기대 동작과 향후 기능 검증 경로를 명시한다.

PROGRESS에는 원인·수정·검증 증거·남은 작업·다음 행동을 남긴다. 의미가 바뀐 결정은 원장·ADR·명세·테스트와 이 카드의 관련 요약을 함께 갱신한다. 단일 문서 기준을 유지하며 코드·schema·seed·모델·평가셋의 revision/hash는 재현을 위해 기록한다.
