# N15 공개 정답 복구와 C11 파생 재채점

현재 상태: [전체8기술검사](n15-local-gate.json)와 [독립 기구 검토](oracle-v4-independent.md) PASS. Python244/server62/domainUI53/UX기구19/type/build와 같은 source 지문에 결속했다. 최종 제품 G5/G6가 아니다.

ADR008은 공식 제조사 출처에서 확인된 두500ml 상품의 구별 정보가 현재 입력에 없는데도 하나만 허용하던 정답 오류를 교정한다. 공개 C02 본 oracle와 C06 첫 턴 oracle의 required_any_skus와 candidate_pool만 같은 두SKU집합으로 바꾼 별도 파일이다. 원래 데이터·입력·분모·clear/uncertain 기준·원본 실행·실패·모델·앱 runtime50은 불변이다. 상품을 자동확정하거나 거래 SKU를 합치지 않는다.

공개23개 실제 run의 목록과 원관측 해시를 전수 확인했다. 영향없는dev30 실행11개는 그대로 두고, baseline/validation12개를 새정답으로 대칭 재채점했다. 이는 새로운 모델 실행이 아니며 비용/호출 추가0이다. C11 원래 두 번째 검증의83/84 FAIL은 보존한다.

| 원래 실제 실행 | 원본 점수 | 새 정답의 파생 점수 | 차이 |
|---|---:|---:|---|
| B0 baseline336 | 227/336 | 228/336 | C02 한 사례 |
| C8 validation | 79/84 | 80/84 | C06 첫 턴의 같은 오류 |
| C11 validation01 | 84/84 | 84/84 | 없음 |
| C11 validation02 | 83/84 | 84/84 | C02 한 사례 |

나머지 공개 실행의 점수는 그대로다. 특히 C9의500ml 요청에340ml를 내놓은 실패는 유지된다. C11두반복을 같은새oracle의 B0 validation59/84와 비교하면 각각개선25/회귀0이다. 같은새oracle의 C5대응01은84,02는83으로 C11의 핵심정상회귀0이며 C10두반복84와도회귀0이다. 정답 교정으로 생긴 점수 차이를 모델 개선으로 주장하지 않는다.

정규화 관측은 질문 원문 등을 제거하고 채점에 필요한 구조와 사용량만 보존한다. 원본과 정규화의old/new 모든점수 일치 검사를 수행했으며, CI가 같은채점을 다시계산한다. 원관측의 비공개 전체 대조는 독립 검토로 별도 확인한다. 초기 기구 초안과 보완 전 반례도 private에 해시와 함께 보존했다. 보호v3의 독립성 영향은 별도 검토에서0이며 원본/동결해시는 불변이다.

다음은 독립검증과 기술PR/CI→같은앱의새source Production/Preview→파생증거와실제두run의최종동결→보호v3최초평가→두역할QA/UX→G5→main/Production G6다. 현재$20/2400과공유장부를 유지한다.
